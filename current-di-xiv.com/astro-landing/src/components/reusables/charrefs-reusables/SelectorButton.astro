---
interface Props {
  buttonText: string;
  buttonID: string;
  buttonHref?: string;
}

const { buttonText, buttonID, buttonHref } = Astro.props as Props;
---

<a
  id={buttonID}
  role="button"
  aria-label={buttonText}
  href={buttonHref}
  target="_blank"
  class="select-none p-2 button-glassbox rounded-md text-nowrap whitespace-nowrap"
  title={buttonText}
>
  <div class="flex items-center">
    <div class="inline-block glassbox p-2 rounded-md">
      <slot name="inline-svg" />
    </div>
    <div class="inline-block">
      <h3 class="regular ml-2 text-lg w1366:text-base w1112:hidden">
        {buttonText}
      </h3>
    </div>
  </div>
</a>
